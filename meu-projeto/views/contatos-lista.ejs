<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  
  <!-- Estilo adicional apenas para a tabela -->
  <style>
    /* Remove espaçamento entre bordas da tabela */
    table {
      border-collapse: collapse;
      width: 100%;
      margin-top: 1rem;
    }

    /* Define borda e espaçamento interno das células */
    th, td {
      border: 1px solid #ddd;
      padding: 8px;
      font-size: 0.95rem;
      vertical-align: top;
    }

    /* Cabeçalhos com fundo cinza claro */
    th {
      background: #f4f4f4;
      text-align: left;
    }

    /* Linhas pares com fundo alternado para melhorar a leitura */
    tbody tr:nth-child(even) {
      background: #fafafa;
    }

    /* Espaço entre links de ação (caso existam futuramente) */
    .actions a {
      margin-right: 8px;
    }
  </style>
</head>

<body>
  <!-- Título principal vindo da rota (title: 'Lista de Contatos') -->
  <h1><%= title %></h1>

  <!-- Links de navegação -->
  <p>
    <a href="/contato">Novo contato</a> |
    <a href="/">Home</a>
  </p>

  <!-- Caso não existam contatos cadastrados -->
  <% if (!contatos || contatos.length === 0) { %>
    <p>Nenhum contato cadastrado ainda.</p>

  <!-- Caso existam registros -->
  <% } else { %>
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Nome</th>
          <th>E-mail</th>
          <th>Idade</th>
          <th>Gênero</th>
          <th>Interesses</th>
          <th>Mensagem</th>
          <th>Criado em</th>
        </tr>
      </thead>

      <tbody>
        <% contatos.forEach(c => { %> <!-- Percorre cada contato retornado do banco -->
          <tr>
            <!-- Exibe cada campo -->
            <td><%= c.id %></td>
            <td><%= c.nome %></td>
            <td><%= c.email %></td>
            <td><%= c.idade || '-' %></td>
            <td><%= c.genero || '-' %></td>

            <!-- Divide os interesses salvos como texto (ex: "node,express") em lista legível -->
            <td>
              <% if (c.interesses) { %>
                <%= c.interesses.split(',').join(', ') %>
              <% } else { %>
                -
              <% } %>
            </td>

            <td><%= c.mensagem %></td>
            <td><%= c.criado_em %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</body>
</html>