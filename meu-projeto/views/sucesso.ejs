<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <h1>✅ <%= title %></h1>
  <p>Recebemos seus dados. Obrigado pelo contato!</p>

  <h2>Resumo do envio</h2>
  <ul>
    <!-- Exibe as principais informações enviadas -->
    <li><strong>Nome:</strong> <%= data.nome %></li>
    <li><strong>E-mail:</strong> <%= data.email %></li>

    <!-- Campo novo: telefone (opcional) -->
    <% if (data.telefone) { %>
      <li><strong>Telefone:</strong> <%= data.telefone %></li>
    <% } %>

    <% if (data.idade) { %>
      <li><strong>Idade:</strong> <%= data.idade %></li>
    <% } %>

    <% if (data.genero) { %>
      <li><strong>Gênero:</strong> <%= data.genero %></li>
    <% } %>

    <!-- Lista de interesses marcados -->
    <% if (Array.isArray(data.interesses) && data.interesses.length) { %>
      <li><strong>Interesses:</strong> <%= data.interesses.join(', ') %></li>
    <% } %>

    <!-- Mensagem enviada -->
    <li><strong>Mensagem:</strong> <%= data.mensagem %></li>

    <!-- Senha: ocultada por segurança, mas confirmando que foi recebida -->
    <% if (data.senha) { %>
      <li><strong>Senha:</strong> (oculta por segurança)</li>
    <% } %>
  </ul>

  <!-- Links de navegação -->
  <p>
    <a href="/contato">Enviar outro</a> |
    <a href="/">Voltar à Home</a> |
    <a href="/contato/lista">Ver contatos cadastrados</a>
  </p>
</body>
</html>