<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <h1>✅ <%= title %></h1>
  <p>Obrigado por se cadastrar, <strong><%= data.nome %></strong>!</p>

  <h2>Resumo do envio:</h2>
  <ul>
    <li><strong>Email:</strong> <%= data.email %></li>

    <% if (data.telefone) { %>
      <li><strong>Telefone:</strong> <%= data.telefone %></li>
    <% } %>

    <% if (data.genero) { %>
      <li><strong>Gênero:</strong> <%= data.genero %></li>
    <% } %>

    <% if (Array.isArray(data.interesses) && data.interesses.length) { %>
      <li><strong>Interesses:</strong> <%= data.interesses.join(', ') %></li>
    <% } %>

    <% if (data.mensagem) { %>
      <li><strong>Mensagem:</strong> <%= data.mensagem %></li>
    <% } %>
  </ul>

  <p>
    <a href="/cadastro">Fazer novo cadastro</a> |
    <a href="/">Voltar à Home</a>
  </p>
</body>
</html>